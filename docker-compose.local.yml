version: "3.9"


x-db-environment: &x-db-environment
  POSTGRES_SSL_MODE: "disable"
  POSTGRES_HOST: "db"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "realworld"
  POSTGRES_PASSWORD: "password"
  POSTGRES_USER: "mhc"

x-backend-app-environment: &x-backend-app-environment
  GOMAXPROCS: "1"
  # App
  APP_NAME: "realworld-fiber-clean"
  APP_VERSION: "1.0.0"
  # HTTP settings
  HTTP_PORT: "8080"
  HTTP_USE_PREFORK_MODE: "false"
  # Logger
  LOG_LEVEL: "debug"
  # PG
  PG_POOL_MAX: "2"
  PG_URL: "postgresql://mhc:password@db:5432/realworld"
  # Metrics
  METRICS_ENABLED: "true"
  # Swagger
  SWAGGER_ENABLED: "true"
  # JWT
  JWT_ISSUER: "realworldfiberclean"
  JWT_SECRET: "c993332e-1a4e-4de5-ac74-98074ff41ff0"
  JWT_EXPIRATION: "240h"
  # CORS
  CORS_ALLOW_ORIGINS: "http://fe.lvh.me"
  CORS_ALLOW_CREDENTIALS: "true"
  CORS_ALLOW_HEADERS: "Origin,Content-Type,Accept,Authorization"
  CORS_ALLOW_METHODS: "GET,POST,HEAD,PUT,DELETE,PATCH"


services:
  db:
    container_name: db
    image: postgres:18.1-alpine
    environment:
      <<: *x-db-environment
    volumes:
      - db_data:/var/lib/postgresql
    ports:
      - "5432:5432"
    networks:
      app_network:
        aliases:
          - db.lvh.me

  app:
    container_name: app
    platform: linux/amd64
    pid: "host"
    build:
      context: .
    environment:
      <<: *x-backend-app-environment
    ports:
      - "8080:8080"
    depends_on:
      - db
    deploy:
      resources:
        limits:
          cpus: '1.00'
          memory: '2Gb'
        reservations:
          cpus: '0.60'
          memory: '1Gb'
    networks:
      app_network:
        aliases:
          - app.lvh.me

  nginx:
    container_name: nginx
    image: nginx:1.29.4-alpine
    # run as 'root' to avoid permissions issues with mounted host files
    user: root
    volumes:
      - ./nginx/nginx.local.conf:/etc/nginx/nginx.conf
      - ./frontend/dist:/var/www/realworld-react
    ports:
      - "80:80"
    depends_on:
      - app
    networks:
      app_network:
        aliases:
          - fe.lvh.me
          - app.lvh.me
          - nginx.lvh.me


networks:
  app_network:
    external: false


volumes:
  db_data:
